{{- if .Values.build.enabled -}}
{{- include "wildfly-common.imagestream" (list . "eap.imagestream") -}}
{{- end -}}

{{- define "eap.imagestream" -}}
metadata:
  name: {{ include "eap73.appImage" . }}
  labels:
    {{- include "eap73.labels" . | nindent 4 }}
{{- end -}}
